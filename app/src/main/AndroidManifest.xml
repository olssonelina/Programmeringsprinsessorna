<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nightingale.qwalk">



    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />


    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".View.LoginActivity"
            android:label="@string/title_activity_maps">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".View.MapsActivity"
            android:parentActivityName=".View.MenuActivity" >
            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".View.MenuActivity" />
        </activity>

        <activity android:name=".View.RegisterActivity"
            android:parentActivityName=".View.LoginActivity" >
            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".View.LoginActivity" />
        </activity>

        <activity android:name=".View.CreateOptionQuestionActivity"
            android:parentActivityName=".View.CreateQuizActivity"
            android:windowSoftInputMode="adjustPan">
            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".View.CreateQuizActivity" />
        </activity>

        <activity android:name=".View.AnswerOptionActivity"
            android:parentActivityName=".View.MapsActivity" >
            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".View.MapsActivity" />
        </activity>

        <activity android:name=".View.FriendActivity"
            android:parentActivityName=".View.MenuActivity" >
            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".View.MenuActivity" />
        </activity>


        <activity android:name=".View.GetPositionActivity"
            android:parentActivityName=".View.CreateOptionQuestionActivity" >
            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".View.CreateOptionQuestionActivity" />
        </activity>


        <activity android:name=".View.CreateQuizActivity"
            android:parentActivityName=".View.MenuActivity" >
            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".View.MenuActivity"
            />

        </activity>

        <activity android:name=".View.MenuActivity"
            android:parentActivityName=".View.LoginActivity" >
            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".View.LoginActivity" />

        </activity>

        <activity android:name=".View.CreateTiebreakerActivity"
            android:parentActivityName=".View.CreateQuizActivity" >
            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".View.CreateQuizActivity" />

        </activity>

        <activity android:name=".View.ShowResultActivity"
            android:parentActivityName=".View.MapsActivity" >
            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".View.MapsActivity" />

        </activity>

        <activity android:name=".View.AnswerTiebreakerActivity"
            android:parentActivityName=".View.MapsActivity" >
            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".View.MapsActivity" />
        </activity>

        <activity android:name=".View.HelpActivity"
            android:parentActivityName=".View.MenuActivity" >
            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".View.MenuActivity" />
        </activity>

        <activity android:name=".View.SettingsActivity"
            android:parentActivityName=".View.MenuActivity" >
            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".View.MenuActivity" />
        </activity>

    </application>

</manifest>